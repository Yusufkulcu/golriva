name: golriva
description: "GOLRIVA — Futbol Zekâsı Düellosu"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10
  supabase_flutter: ^2.8.0
  google_mobile_ads: ^5.2.0 # odullu reklam (yalniz Android/iOS'ta etkin)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/brand/
    - assets/icons/
